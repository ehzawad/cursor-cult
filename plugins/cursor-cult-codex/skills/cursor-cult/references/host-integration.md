# Host integration

Codex invokes the packaged runner from the skill directory, normally waits for foreground completion, and uses managed background lifecycle commands only when the user explicitly requests detached work.
