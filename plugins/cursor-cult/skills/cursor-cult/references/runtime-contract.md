# Runtime contract

The runner validates host-created roles and the Intent Capsule, strips Cursor API-key variables by default, verifies browser-login routing, requires a non-empty terminal result, isolates partial failures, scopes sessions by project/host/role, and supports foreground plus managed-background lifecycles.
